# TODO

## Future work

- Port extension runtime code to TypeScript with a minimal build step and type-checked browser API wrappers.
- Add GitHub Actions release automation for dual-browser builds and store publishing after first manual store approvals.
